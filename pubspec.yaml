name: backend
description: An new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  crypto: ^3.0.3
  dart_frog: ^1.0.0
  freezed_annotation: ^2.4.1
  stormberry: ^0.13.1
  shared_models:
    path: 'packages/shared_models'
  db_models:
    path: 'packages/db_models'
  dotenv: ^4.2.0
  email_sender: ^1.3.3
  #http: ^1.1.0

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^2.4.5
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
